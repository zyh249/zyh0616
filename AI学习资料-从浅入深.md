# AI 学习资料：从基础概念到机器学习、深度学习与 NLP

> 适合放到 GitHub 的学习笔记版本。  
> 说明：本资料根据你上传的课件重新梳理和改写，建议公开发布时只发布自己的整理笔记，不要直接上传未经授权的课件原件、截图或大量原文。

## 目录

1. [学习路线总览](#01-学习路线总览)
2. [数学基础](#02-数学基础)
3. [机器学习入门](#03-机器学习入门)
4. [机器学习任务、流程与评估](#04-机器学习任务流程与评估)
5. [常见机器学习算法](#05-常见机器学习算法)
6. [集成学习](#06-集成学习)
7. [深度学习入门](#07-深度学习入门)
8. [神经网络基础](#08-神经网络基础)
9. [卷积神经网络 CNN](#09-卷积神经网络-cnn)
10. [循环神经网络 RNN 与 NLP](#10-循环神经网络-rnn-与-nlp)
11. [文本预处理](#11-文本预处理)
12. [实践项目路线](#12-实践项目路线)

---

## 01. 学习路线总览

### 1.1 先建立整体地图

AI 学习可以从四个层次理解：

```text
人工智能 AI
└── 机器学习 ML
    └── 神经网络 NN
        └── 深度学习 DL
```

- **人工智能 AI**：让机器表现出类似人的智能，包括规则系统、搜索、推理、机器学习等。
- **机器学习 ML**：让机器从数据中学习规律，而不是完全依赖人手写规则。
- **神经网络 NN**：机器学习中的一类模型，模仿神经元连接结构，通过大量参数表达复杂关系。
- **深度学习 DL**：多层神经网络，通过层级结构自动学习更复杂、更抽象的特征。

初学时可以记住一句话：**机器学习关注“从数据中学规律”，深度学习关注“用多层神经网络自动提取复杂特征”。**

### 1.2 推荐学习顺序

```text
数学基础
→ 机器学习基本概念
→ 机器学习任务与建模流程
→ 传统机器学习算法
→ 集成学习
→ 深度学习与神经网络
→ CNN 图像任务
→ RNN / NLP 文本任务
→ 项目实践
```

### 1.3 这套资料的学习目标

学完后你应该能做到：

- 看懂 AI、机器学习、神经网络、深度学习之间的关系；
- 理解样本、特征、标签、模型、算法、参数、超参数；
- 知道监督学习、无监督学习、半监督学习、强化学习的区别；
- 能按“数据处理 → 特征工程 → 模型训练 → 模型评估 → 部署”的流程做一个简单机器学习项目；
- 理解决策树、随机森林、GBDT、XGBoost 等算法的核心思想；
- 能看懂神经网络中的前向传播、损失函数、反向传播、梯度下降；
- 能理解 CNN 适合图像，RNN 适合序列和文本；
- 能搭建简单的 AI 项目练习路线。

---

## 02. 数学基础

数学不需要一开始学得很深，但要先掌握“够用”的部分。

### 2.1 指数与对数

指数函数和对数函数常用于概率、损失函数、激活函数和信息熵。

常见知识点：

```text
e^0 = 1
e^1 = e
a^m * a^n = a^(m+n)
a^m / a^n = a^(m-n)
log(1) = 0
```

在机器学习里，对数经常出现在：

- 交叉熵损失函数；
- 信息熵、信息增益；
- 概率模型；
- 模型评估中的对数损失。

### 2.2 线性函数

线性函数：

```text
y = a + bx
```

在机器学习中通常可以理解为：

```text
预测值 = 偏置 + 权重 * 特征
```

也就是：

```text
y_hat = b + w * x
```

其中：

- `b`：偏置 bias；
- `w`：权重 weight；
- `x`：输入特征；
- `y_hat`：模型预测值。

线性回归、逻辑回归和神经网络的基本计算都可以从这个公式理解。

### 2.3 二次函数

二次函数：

```text
y = a + bx + cx^2
```

它常用于理解“损失函数的曲线”和“最小值”。

如果损失函数是一条碗形曲线，那么模型训练的目标就是找到曲线最低点。

### 2.4 导数、斜率与梯度

导数可以理解为某一点处函数变化的快慢，也可以理解为曲线在该点的切线斜率。

```text
dy / dx = 斜率 = 梯度
```

机器学习中，梯度告诉我们：

```text
参数往哪个方向调整，损失函数会下降得更快
```

梯度下降的核心公式：

```text
新参数 = 旧参数 - 学习率 * 梯度
```

### 2.5 求和符号

求和符号可以理解为“循环累加”。

```text
Σ 表示把一组数加起来
```

比如损失函数经常写成：

```text
Loss = Σ(真实值 - 预测值)^2
```

意思就是：对每个样本计算误差，再把所有样本的误差加起来。

### 2.6 本章练习

1. 画出 `y = x^2` 和 `y = 2x + 1` 的图像。
2. 手算 `y = x^2` 在 `x = 2` 处的导数。
3. 用 Python 写一个 for 循环实现求和。
4. 用 NumPy 计算一组预测值和真实值的 MSE。

---

## 03. 机器学习入门

### 3.1 机器学习解决什么问题

传统编程是：

```text
规则 + 数据 → 答案
```

机器学习更像是：

```text
数据 + 答案 → 规则
规则 + 新数据 → 新答案
```

也就是说，机器学习先通过已有数据训练出模型，再用模型预测新数据。

### 3.2 数据相关术语

| 术语 | 含义 | 例子 |
|---|---|---|
| 样本 sample | 一条完整数据 | 一个学生、一张图片、一句话 |
| 特征 feature | 用来预测的属性 | 身高、体重、考试分数 |
| 标签 label / target | 要预测的结果 | 薪资、类别、是否患病 |
| 训练集 train set | 用来训练模型的数据 | 70% 数据 |
| 测试集 test set | 用来最终评估模型的数据 | 20% 数据 |
| 验证集 validation set | 用来调参的数据 | 10% 数据 |

### 3.3 模型相关术语

| 术语 | 含义 |
|---|---|
| 算法 algorithm | 学习规律的方法，如线性回归、决策树 |
| 模型 model | 算法训练后得到的预测器 |
| 参数 parameter | 模型自动学习到的值，如权重、偏置 |
| 超参数 hyperparameter | 人为设定的配置，如学习率、树深度、K 值 |
| 损失函数 loss function | 衡量预测值和真实值差距 |
| 性能指标 metric | 衡量模型好坏，如准确率、F1、RMSE |

可以记住：

```text
模型 = 算法 + 参数
```

参数通常由计算机通过训练得到；超参数通常由人通过验证集和经验来选择。

### 3.4 拟合问题

| 状态 | 表现 | 解决方向 |
|---|---|---|
| 欠拟合 underfitting | 训练集和测试集都差 | 增加特征、换更强模型、训练更久 |
| 过拟合 overfitting | 训练集好，测试集差 | 正则化、剪枝、增加数据、降低模型复杂度 |
| 拟合合适 | 训练和测试表现都较好 | 保持并验证泛化能力 |

初学时要重点理解：**模型不是在训练集上越准越好，而是要在新数据上表现好。**

---

## 04. 机器学习任务、流程与评估

### 4.1 任务类型

机器学习任务可以先按是否有标签分为：

```text
监督学习：有特征，有标签
无监督学习：有特征，无标签
半监督学习：少量有标签，大量无标签
强化学习：智能体通过奖励学习行动策略
```

再按目标分为：

| 任务 | 输出 | 例子 | 常见算法 |
|---|---|---|---|
| 回归 | 连续数值 | 房价、薪资、温度 | 线性回归、随机森林、XGBoost |
| 分类 | 离散类别 | 是否垃圾邮件、图片类别 | 逻辑回归、决策树、随机森林 |
| 聚类 | 自动分组 | 用户分群 | K-means |

### 4.2 建模流程

一个完整机器学习项目通常按这个流程走：

```text
1. 获取数据
2. 数据基本处理
3. 特征工程
4. 模型训练
5. 模型评估
6. 模型部署
```

#### 1. 获取数据

数据可以是：

- 表格数据；
- 图像数据；
- 文本数据；
- 日志数据；
- 传感器数据。

#### 2. 数据基本处理

常见处理：

- 缺失值处理；
- 异常值处理；
- 类别编码；
- 数值归一化；
- 数据切分。

#### 3. 特征工程

特征工程是传统机器学习里很重要的环节，包括：

- 特征提取；
- 特征预处理；
- 特征选择；
- 特征降维；
- 特征归一化。

#### 4. 模型训练

选择算法，输入训练数据，让模型学习参数。

#### 5. 模型评估

根据任务选择指标：

- 回归：MAE、MSE、RMSE、R²；
- 分类：准确率、精确率、召回率、F1、AUC；
- 聚类：轮廓系数等。

#### 6. 模型部署

把模型保存并提供服务，比如：

- 本地脚本；
- API 服务；
- Web 应用；
- CPU / GPU 推理服务。

### 4.3 分类指标

二分类中常见四个结果：

| 名称 | 含义 |
|---|---|
| TP 真阳 | 真实为正，预测为正 |
| FP 假阳 | 真实为负，预测为正 |
| FN 假阴 | 真实为正，预测为负 |
| TN 真阴 | 真实为负，预测为负 |

常见指标：

```text
准确率 accuracy = 正确数量 / 总样本数量
精确率 precision = TP / (TP + FP)
召回率 recall = TP / (TP + FN)
F1 = 2 * precision * recall / (precision + recall)
```

如何理解：

- **精确率**：模型说“是”的时候，有多少是真的；
- **召回率**：真实为“是”的样本，有多少被找出来；
- **F1**：综合精确率和召回率，尤其适合样本不平衡的任务。

---

## 05. 常见机器学习算法

### 5.1 算法总览

| 算法 | 适合任务 | 核心思想 |
|---|---|---|
| KNN | 回归 / 分类 | 看最近的 K 个邻居投票 |
| 朴素贝叶斯 | 分类 | 基于概率和条件独立假设 |
| SVM | 分类 | 找到最大间隔的分类边界 |
| 线性回归 | 回归 | 用直线拟合连续数值 |
| 逻辑回归 | 分类 | 线性结果经过 sigmoid 得到概率 |
| 决策树 | 回归 / 分类 | 按特征不断划分数据 |
| 随机森林 | 回归 / 分类 | 多棵决策树投票 |
| GBDT / XGBoost | 回归 / 分类 | 一棵棵树修正前面的错误 |
| K-means | 聚类 | 根据距离把样本分成 K 组 |
| 全连接神经网络 | 分类 / 回归 | 多层线性变换 + 激活函数 |

### 5.2 线性回归

用于预测连续值：

```text
y_hat = b + w1*x1 + w2*x2 + ... + wn*xn
```

训练目标是让预测值尽可能接近真实值。

常用损失：

```text
MSE = 平均平方误差
MAE = 平均绝对误差
```

### 5.3 逻辑回归

虽然叫“回归”，但常用于分类。

核心流程：

```text
线性计算 z = b + w*x
概率输出 y = sigmoid(z)
```

sigmoid 把任意实数压缩到 0 到 1 之间，可理解为属于某类的概率。

### 5.4 决策树

决策树是一种树形结构：

- 节点：一个特征判断；
- 分支：判断结果；
- 叶子：最终类别或预测值。

构建决策树时，核心是选择“最能区分类别”的特征。常见方法：

- ID3：信息增益；
- CART：基尼系数；
- 剪枝：缓解决策树过拟合。

### 5.5 K-means 聚类

K-means 是无监督学习算法：

1. 设定 K 个簇；
2. 随机初始化中心点；
3. 每个样本分配到最近的中心点；
4. 重新计算中心点；
5. 重复直到稳定。

适合做用户分群、相似样本划分等。

---

## 06. 集成学习

### 6.1 集成学习思想

集成学习的核心是：

```text
多个弱学习器组合成一个强学习器
```

就像“多个人投票比一个人判断更稳”。

常见集成方式：

```text
Bagging
Boosting
```

### 6.2 Bagging

Bagging 的特点：

- 对训练数据进行有放回抽样；
- 每个模型独立训练；
- 模型之间可以并行；
- 最终通过平权投票或平均得到结果。

代表算法：

```text
随机森林 Random Forest
```

随机森林的流程：

1. 有放回抽样生成多份训练集；
2. 每棵树随机选择部分特征；
3. 训练多棵决策树；
4. 分类任务多数投票，回归任务取平均。

### 6.3 Boosting

Boosting 的特点：

- 使用全部样本；
- 后一个模型重点学习前一个模型没学好的部分；
- 模型串行训练；
- 最终加权投票或加权求和。

代表算法：

```text
GBDT
XGBoost
```

### 6.4 Bagging vs Boosting

| 对比项 | Bagging | Boosting |
|---|---|---|
| 数据 | 有放回抽样 | 全部样本 |
| 训练方式 | 并行 | 串行 |
| 关注点 | 降低方差 | 降低偏差、逐步修正错误 |
| 投票方式 | 平权投票 | 加权投票 |
| 代表算法 | 随机森林 | GBDT、XGBoost |

---

## 07. 深度学习入门

### 7.1 为什么需要深度学习

传统机器学习通常依赖人工特征工程。比如做图片分类时，人可能要设计边缘、颜色、纹理等特征。

深度学习的优势是：

```text
从原始数据中自动提取特征
```

尤其适合处理：

- 图像；
- 语音；
- 文本；
- 多模态数据。

### 7.2 深度学习的特点

| 特点 | 解释 |
|---|---|
| 多层非线性变换 | 层数越深，可表达越复杂的特征 |
| 自动特征提取 | 减少人工特征工程 |
| 适合高维数据 | 图像、语音、文本都属于高维数据 |
| 需要大量数据和算力 | 常需要 GPU |
| 可解释性较弱 | 很多深度模型像黑盒 |

### 7.3 常见深度学习模型

| 模型 | 主要用途 |
|---|---|
| 全连接神经网络 DNN | 表格分类、简单分类 |
| CNN | 图像分类、目标检测、图像分割 |
| RNN / LSTM / GRU | 文本、时间序列 |
| Transformer | 现代 NLP 和大语言模型基础 |
| GAN | 图像生成 |
| AutoEncoder | 降维、异常检测 |
| GNN | 图结构数据 |

---

## 08. 神经网络基础

### 8.1 神经元的基本计算

一个神经元通常做两件事：

```text
1. 线性计算：z = b + w1*x1 + w2*x2 + ...
2. 激活函数：y = activation(z)
```

没有激活函数，多层线性网络仍然等价于一层线性变换，无法表达复杂非线性关系。

### 8.2 常见激活函数

| 激活函数 | 特点 |
|---|---|
| sigmoid | 输出 0 到 1，适合二分类概率 |
| tanh | 输出 -1 到 1 |
| ReLU | 常用，简单高效 |
| Softmax | 多分类输出概率分布 |

### 8.3 前向传播

前向传播就是数据从输入层一路算到输出层：

```text
输入 → 隐藏层 → 输出层 → 预测结果
```

它解决的是“模型给出的预测是什么”。

### 8.4 损失函数

损失函数衡量预测值和真实值的差异。

| 任务 | 常见损失 |
|---|---|
| 回归 | MSE、MAE |
| 二分类 | Binary Cross Entropy |
| 多分类 | Cross Entropy / Softmax Loss |

### 8.5 反向传播

反向传播解决的是“每个参数应该怎么改”。

流程：

```text
前向传播得到预测
→ 损失函数计算误差
→ 从输出层往前计算梯度
→ 梯度下降更新参数
```

参数更新公式：

```text
W_new = W_old - learning_rate * gradient
```

### 8.6 优化与学习率

学习率太大，可能震荡甚至不收敛；学习率太小，训练速度慢。

常见优化器：

- SGD；
- Momentum；
- RMSProp；
- Adam。

常见学习率策略：

- 固定学习率；
- 学习率衰减；
- 根据验证集表现动态调整。

### 8.7 正则化

正则化用于缓解过拟合。

常见方法：

| 方法 | 作用 |
|---|---|
| L1 / L2 | 限制参数过大 |
| Dropout | 训练时随机让部分神经元失活 |
| BatchNorm | 对中间数据标准化，再缩放和平移 |
| 早停 Early Stopping | 验证集变差时停止训练 |

### 8.8 PyTorch 模型搭建套路

```python
import torch
import torch.nn as nn

class Model(nn.Module):
    def __init__(self):
        super().__init__()
        self.net = nn.Sequential(
            nn.Linear(10, 32),
            nn.ReLU(),
            nn.Linear(32, 2)
        )

    def forward(self, x):
        return self.net(x)
```

核心记法：

```text
__init__ 定义层
forward 定义数据怎么流动
```

---

## 09. 卷积神经网络 CNN

### 9.1 图像基础

图像可以理解为像素矩阵。

常见图像类型：

| 类型 | 说明 |
|---|---|
| 二值图 | 像素只有 0 和 1 |
| 灰度图 | 像素范围 0 到 255 |
| 彩色图 | RGB 三通道 |
| HWC | 高度 Height、宽度 Width、通道 Channel |

RGB 图像可以看成三张矩阵叠在一起：

```text
R 通道 + G 通道 + B 通道
```

### 9.2 CNN 的组成

CNN 主要由三部分组成：

```text
卷积层 → 池化层 → 全连接层
```

| 层 | 作用 |
|---|---|
| 卷积层 | 提取局部特征 |
| 激活层 | 增强非线性表达 |
| 池化层 | 降低尺寸和参数量 |
| 全连接层 | 输出分类结果 |

### 9.3 卷积层

卷积的本质是：

```text
卷积核与图像局部区域做点积
```

卷积层可以自动提取边缘、纹理、形状等局部特征。

### 9.4 池化层

池化层常用于降维。

常见池化：

- 最大池化 Max Pooling；
- 平均池化 Average Pooling。

最大池化会保留局部区域中最显著的特征。

### 9.5 CNN 图像分类流程

```text
图片读取
→ 转为张量
→ 卷积提取特征
→ 池化降维
→ 全连接输出类别
→ 计算损失
→ 反向传播更新参数
```

---

## 10. 循环神经网络 RNN 与 NLP

### 10.1 什么是序列数据

序列数据的特点是：

```text
后面的数据与前面的数据有关
```

例子：

- 文字序列；
- 时间序列；
- 语音序列；
- 股票价格；
- 天气数据。

### 10.2 RNN 的特点

RNN 是专门处理序列数据的神经网络。

它和普通前馈网络的区别是：

```text
RNN 会保留前面时间步的信息
```

适合：

- 文本生成；
- 语言建模；
- 情感分析；
- 机器翻译；
- 时间序列预测；
- 语音识别。

### 10.3 词嵌入

文本不能直接输入神经网络，需要数值化。

词嵌入的作用是：

```text
把 token 映射成向量
```

例如：

```text
“猫” → [0.12, -0.88, 0.31, ...]
```

词嵌入层可以：

- 把离散词元转为连续向量；
- 降低维度；
- 捕捉语义相似性。

PyTorch 中可使用：

```python
nn.Embedding(num_embeddings=词表大小, embedding_dim=向量维度)
```

### 10.4 RNN 文本任务流程

```text
文本
→ 分词 / tokenization
→ 词表索引
→ 词嵌入向量
→ RNN / LSTM / GRU
→ 全连接层
→ 输出结果
```

---

## 11. 文本预处理

### 11.1 NLP 的基本任务

自然语言处理 NLP 可以分为：

| 类型 | 例子 |
|---|---|
| 自然语言理解 NLU | 文本分类、情感分析、实体识别 |
| 自然语言生成 NLG | 机器翻译、摘要生成、问答、故事扩写 |
| 语音文字任务 | 语音识别、语音合成 |

### 11.2 文本预处理的目的

文本预处理的核心目标是：

```text
准备模型需要的 X 和 Y
```

也就是：

- 把文本转成模型能读的数字；
- 规范输入长度；
- 检查标签分布；
- 清理脏数据；
- 提取文本特征。

### 11.3 文本预处理主要环节

```text
1. 基本处理：分词、词性标注、命名实体识别
2. 语料分析：标签分布、句子长度、词频统计
3. 特征处理：n-gram、长度规范
4. 文本编码：词频编码、词嵌入、预训练向量
5. 数据增强：回译
```

### 11.4 分词

分词是把连续文字切成词元序列。

中文需要分词，因为中文没有天然空格分隔。

例子：

```text
我在学习人工智能
→ 我 / 在 / 学习 / 人工智能
```

常见工具：

- jieba；
- SnowNLP；
- THULAC；
- PyLTP。

### 11.5 词性标注与命名实体识别

- **词性标注**：判断每个词是名词、动词、形容词等；
- **命名实体识别 NER**：识别人名、地名、组织名、时间、金额等实体。

### 11.6 文本编码

常见编码方式：

| 编码 | 特点 |
|---|---|
| One-hot | 简单但维度高 |
| 词频编码 | 统计词出现次数 |
| TF-IDF | 强调重要词，降低常见词权重 |
| 词嵌入 | 将词映射为低维稠密向量 |
| 预训练词向量 | 使用 Word2Vec、GloVe 等已有向量 |

### 11.7 文本长度规范

批量训练时，文本长度通常需要统一：

- 太长：截断；
- 太短：padding 补齐。

### 11.8 n-gram 特征

n-gram 用于捕捉相邻词组合。

例如：

```text
我 喜欢 机器 学习
2-gram:
我_喜欢
喜欢_机器
机器_学习
```

### 11.9 数据增强

文本数据增强可以使用回译：

```text
中文 → 英文 → 中文
```

优点：简单、能增加样本量。  
缺点：短文本可能变化不大，多次翻译可能语义失真。

---

## 12. 实践项目路线

### 12.1 初级项目

| 项目 | 目标 |
|---|---|
| 线性回归预测房价 | 掌握回归流程 |
| 鸢尾花分类 | 掌握分类流程 |
| K-means 用户分群 | 掌握无监督学习 |
| 决策树可视化 | 理解树模型 |

### 12.2 中级项目

| 项目 | 目标 |
|---|---|
| 随机森林预测薪资 | 理解 Bagging |
| XGBoost 分类 | 理解 Boosting |
| 新闻文本分类 | 掌握文本预处理 |
| CIFAR10 图像分类 | 掌握 CNN |

### 12.3 深度学习项目

| 项目 | 目标 |
|---|---|
| 手写数字识别 MNIST | 掌握 DNN / CNN |
| 图片分类 | 掌握 CNN 训练流程 |
| 简单文本生成 | 掌握 Embedding + RNN |
| 情感分析 | 掌握 NLP 分类任务 |

### 12.4 推荐 GitHub 目录结构

```text
ai-learning-notes/
├── README.md
├── docs/
│   ├── 00-roadmap.md
│   ├── 01-math-basic.md
│   ├── 02-machine-learning-basic.md
│   ├── 03-ml-workflow-metrics.md
│   ├── 04-ml-algorithms.md
│   ├── 05-ensemble-learning.md
│   ├── 06-deep-learning-basic.md
│   ├── 07-neural-network-basic.md
│   ├── 08-cnn.md
│   ├── 09-rnn-nlp.md
│   └── 10-projects.md
├── code/
│   ├── ml/
│   ├── dl/
│   └── nlp/
├── assets/
│   └── images/
├── references.md
└── LICENSE
```

---

## 最后总结

这批课件可以整理成三条主线：

```text
主线一：机器学习基础
AI 概念 → 数学基础 → 样本/特征/标签 → 建模流程 → 评估指标

主线二：传统机器学习算法
线性回归 → 逻辑回归 → 决策树 → 随机森林 → GBDT / XGBoost → K-means

主线三：深度学习与文本图像任务
神经网络 → 损失函数 → 反向传播 → CNN 图像 → RNN 序列 → NLP 文本预处理
```

学习建议：

1. 先把概念串起来，不要一开始死磕公式。
2. 每章配一个小代码案例。
3. GitHub 文档里尽量用自己的语言写解释，不要直接复制课件原文。
4. 每个算法都按“解决什么问题 → 核心思想 → 训练流程 → 适用场景 → 简单代码”来写。
